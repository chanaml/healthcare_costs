import {constant} from "d3plus-common";

import {default as Plot} from "./Plot";

/**
    @class BarChart
    @extends Plot
    @desc Creates a line plot based on an array of data.
    @example <caption>the equivalent of calling:</caption>
new d3plus.Plot()
  .discrete("x")
  .shape("Line")
*/
var BarChart = (function (Plot) {
  function BarChart() {

    Plot.call(this);
    this._baseline = 0;
    this._discrete = "x";
    this._shape = constant("Bar");
    this.x("x");

  }

  if ( Plot ) BarChart.__proto__ = Plot;
  BarChart.prototype = Object.create( Plot && Plot.prototype );
  BarChart.prototype.constructor = BarChart;

  return BarChart;
}(Plot));

export default BarChart;

//# sourceMappingURL=BarChart.js.map