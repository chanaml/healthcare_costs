import {constant} from "d3plus-common";

import {default as Plot} from "./Plot";

/**
    @class AreaPlot
    @extends Plot
    @desc Creates an area plot based on an array of data.
    @example <caption>the equivalent of calling:</caption>
new d3plus.Plot()
  .baseline(0)
  .discrete("x")
  .shape("Area")
*/
var AreaPlot = (function (Plot) {
  function AreaPlot() {

    Plot.call(this);
    this._baseline = 0;
    this._discrete = "x";
    this._shape = constant("Area");
    this.x("x");

  }

  if ( Plot ) AreaPlot.__proto__ = Plot;
  AreaPlot.prototype = Object.create( Plot && Plot.prototype );
  AreaPlot.prototype.constructor = AreaPlot;

  return AreaPlot;
}(Plot));

export default AreaPlot;

//# sourceMappingURL=AreaPlot.js.map